{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_resetting_reset', {'token': token}), 'attr': { 'class': 'fos_user_resetting_reset form-signin' } }) }}
    <h2 class="form-signin-heading">Réinitialisaion de votre mot de passe</h2>
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-lg btn-primary-outline btn-block" type="submit" value="{{ 'resetting.reset.submit'|trans }}" />
    </div>
{{ form_end(form) }}
